# MultiMER — Python dependencies
#
# Versions are the ones the submitted system was run with; they are lower
# bounds, not exhaustively tested minima. Python 3.9 or newer.
#
# torch is intentionally unpinned as to build: install the wheel matching your
# CUDA version first (https://pytorch.org/get-started/locally/), then this file.

# --- core ---
torch>=2.8.0
transformers>=4.57.6
accelerate>=1.10.1          # device_map="auto" in the local-inference scripts
peft>=0.17.1                # LoRA fine-tuning and adapter loading
bitsandbytes>=0.48.2        # 4-bit NF4 loading (predict_lora_audio.py --load_in_4bit)
safetensors>=0.7.0
sentencepiece>=0.2.1

# --- data / numerics ---
numpy>=2.0.2
pandas>=2.3.3
tqdm>=4.67.3

# --- media I/O ---
opencv-python>=5.0.0.93     # video frame extraction for the vision voters
pillow>=11.3.0
soundfile>=0.12.1           # WAV loading for the audio voters

# --- commercial API voters ---
openai>=2.30.0              # GPT-4o Audio, GPT-5.5, and the Alibaba
                            # OpenAI-compatible endpoint (Qwen family)
anthropic>=0.113.0          # Claude Sonnet 5
requests>=2.32.5            # Gemini REST endpoint
